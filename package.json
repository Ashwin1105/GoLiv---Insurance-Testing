{
  "name": "insureco-portal-tests",
  "version": "1.0.0",
  "description": "Playwright + Cucumber BDD tests for InsureCo Portal",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require ./steps/**/*.ts",
    "report": "allure generate allure-results --clean -o allure-report && allure open"
  },
  "dependencies": {
    "@playwright/test": "^1.45.0",
    "@cucumber/cucumber": "^10.9.0",
    "allure-cucumberjs": "^3.10.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=14"
  }
}
